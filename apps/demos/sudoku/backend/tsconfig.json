{
  "extends": "../../../../tsconfig.json",
  "include": ["**/*.ts"],
  "compilerOptions": {
    "types": ["jest", "node"],
    "module": "esnext",
    "moduleResolution": "bundler",
    // Emit options
    "noEmit": false // Temporary while in development
  }
}
