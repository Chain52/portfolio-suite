{
  "extends": "../../../../tsconfig.json",
  "include": ["**/*.ts", "jest.config.js"],
  "compilerOptions": {
    "types": ["jest", "node"],
    "module": "esnext",
    "moduleResolution": "bundler",
    // Emit options
    "noEmit": false // Temporary while in development
  }
}
