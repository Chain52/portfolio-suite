import SolutionTracer from '../SolutionTracer';
import { Technique } from '../types';

export default class NakedSingle implements Technique {
  apply(tracer: SolutionTracer): void {
    // TODO: check if possible add a trace to the tracer if so
    return;
  }
}
